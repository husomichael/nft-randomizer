/*
Future component to add.
This component will be a "Next Steps" page after the "Results" page. 
This component will be a tutorial / walkthrough of how to implement a CSV to a photoshop project 
to get it to print out jpegs based on the CSV file.
*/

function NextSteps(){

    return(
        <div>

        </div>
    );
};

export default NextSteps;